## Scope

Management CLI for a CELINE Superset instance or migration between different envs.

`instances.example.yaml` contains key details per instance.

Commands

- `bootstrap` default to local env and configure database
- `chart` and `dashboard` allow to `import` and `export` per env